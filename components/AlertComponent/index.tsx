import { ReactNode } from "react";

type Props = {
    children: ReactNode
    title: string
}

export const WarningAlert = ({children, title}: Props) => {
    
}

export const SuccessAlert = ({children, title}: Props) => {

}

export const DangerAlert = ({children, title}: Props) => {

}

export const TipsAlert = ({children, title}: Props) => {
    
}